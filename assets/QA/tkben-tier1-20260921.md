# TKBEN Tier 1 validation record

Last updated: 2026-09-21

## Scope

- Revision: `c48eefa8bad8e85d1bc18bcb8cf22042e1f72b38`
- Branch: `develop`
- Environment: Windows, Python 3.14.7, Node.js 22.23.1, npm 10.9.8
- Launcher: `start_on_windows.ps1 -Launch`
- Evidence policy: text-only; no screenshots, videos, browser bundles, or
  secrets are retained in this artifact.

## Results

| Slice | Result | Evidence |
| --- | --- | --- |
| T1-01 startup and route shell | PASS | `test_startup_loading.py`: 2 passed with `--browser-channel=chrome`; in-app browser rendered Dataset and Settings after restart. |
| T1-02 runtime Settings | PARTIAL | `test_settings_ui.py`: 1 passed with `--browser-channel=chrome`; browser cross-field validation/recovery passed; one override survived launcher restart; reset-all restored histogram default and removed `app/resources/runtime-settings.json`. Full all-16 boundary sweep remains. |
| T1-03 key management | PARTIAL | Synthetic live lifecycle passed duplicate `409`, active-key switching, active-delete guard `400`, reveal policy `403`, masked responses, and plaintext-absence check. Synthetic rows were removed; final `/api/keys` count was `0`. Rendered UI lifecycle and direct DB ciphertext inspection remain. |
| T1-04 SQLite/restart safety | PASS | Focused backend contracts: `55 passed, 13 warnings`; runtime override survived a real launcher restart and was then reset. PostgreSQL is separate. |
| T1-05 catalog filtering/races | PARTIAL | Dataset stale-response browser scenario: `1 passed`; frontend units: `14 files, 60 tests passed`. Full populated filter matrix remains. |

## Commands and outcomes

```text
.\start_on_windows.ps1 -Launch                         PASS
$env:PYTHONPATH='app'; .\app\server\.venv\Scripts\python.exe -m pytest -c app/tests/pytest.ini app/tests/e2e/test_settings_ui.py -q --browser-channel=chrome PASS (1 passed)
$env:PYTHONPATH='app'; .\app\server\.venv\Scripts\python.exe -m pytest -c app/tests/pytest.ini app/tests/e2e/test_startup_loading.py -q --browser-channel=chrome PASS (2 passed)
$env:PYTHONPATH='app'; .\app\server\.venv\Scripts\python.exe -m pytest -c app/tests/pytest.ini app/tests/e2e/test_app_flow.py::TestDatasetPage::test_catalog_race_keeps_loading_owned_by_newest_request -q --browser-channel=chrome PASS (1 passed)
$env:PYTHONPATH='app'; .\app\server\.venv\Scripts\python.exe -m pytest -c app/tests/pytest.ini app/tests/unit/server/configurations/test_runtime_settings.py app/tests/unit/server/api/test_settings_routes.py app/tests/unit/server/api/test_keys_routes.py app/tests/unit/server/repositories/test_database_initialization.py app/tests/unit/server/repositories/test_database_migrations.py app/tests/unit/server/repositories/test_persistence_contract.py -q PASS (55 passed, 13 warnings)
.\runtimes\nodejs\npm.cmd --prefix .\app\client run test:unit PASS (14 files, 60 tests)
```

The first default Playwright run was not an application failure: the
repository-local Chromium executable was absent. The installed Chrome channel
was used for the passing browser runs. The official `-KillAll` command hit
Windows `Access denied` while inspecting process command lines; only the exact
launcher-owned process trees were stopped explicitly afterward.

## Final cleanup state

- Runtime settings: defaults active, no overrides, runtime-settings file absent.
- Hugging Face keys: zero rows after synthetic lifecycle cleanup.
- Ports 5000 and 8000: checked after cleanup; no unrelated processes were
  targeted.

Remaining work is tracked in the canonical
[project status ledger](../docs/project_status_ledger.md#validation-campaign-roadmap).
