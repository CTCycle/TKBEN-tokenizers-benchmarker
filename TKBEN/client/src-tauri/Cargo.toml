[package]
name = "tkben-desktop"
version = "3.1.0"
description = "TKBEN desktop shell"
authors = ["TKBEN"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
