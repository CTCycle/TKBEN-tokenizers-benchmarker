import { DndContext, KeyboardSensor, PointerSensor, closestCenter, useSensor, useSensors, type DragEndEvent } from '@dnd-kit/core';
import { SortableContext, rectSortingStrategy, sortableKeyboardCoordinates, useSortable } from '@dnd-kit/sortable';
import { CSS } from '@dnd-kit/utilities';
import { useRef, useState, type RefObject } from 'react';
import BenchmarkRunWizard from '../components/BenchmarkRunWizard';
import { BenchmarkMetricWidget } from '../components/benchmark-dashboard/BenchmarkMetricWidget';
import DashboardExportButton from '../components/DashboardExportButton';
import DismissibleBanner from '../components/DismissibleBanner';
import { moveDashboardWidget } from '../features/benchmark-dashboard/benchmarkDashboardLayout';
import { useBenchmarkDashboardLayout } from '../hooks/useBenchmarkDashboardLayout';
import { useBenchmarkWorkspace, type BenchmarkRunPayload } from '../hooks/useBenchmarkWorkspace';
import type { BenchmarkDashboardWidgetData } from '../types/api';

const SortableWidget = ({ widget }: { widget: BenchmarkDashboardWidgetData }) => {
  const { attributes, listeners, setNodeRef, transform, transition, isDragging } = useSortable({ id: widget.widget_id });
  return <div ref={setNodeRef} className="benchmark-dashboard-sortable" style={{ transform: CSS.Transform.toString(transform), transition }} {...attributes} {...listeners} data-dragging={isDragging || undefined}><BenchmarkMetricWidget widget={widget} /></div>;
};

const Customizer = ({ widgets, visibleIds, onApply, onClose, trigger }: { widgets: BenchmarkDashboardWidgetData[]; visibleIds: string[]; onApply: (ids: string[]) => void; onClose: () => void; trigger: RefObject<HTMLButtonElement | null> }) => { const [draft, setDraft] = useState(visibleIds); const groups = widgets.reduce<Record<string, BenchmarkDashboardWidgetData[]>>((result, widget) => { (result[widget.category_label] ??= []).push(widget); return result; }, {}); const close = () => { onClose(); trigger.current?.focus(); }; return <div className="benchmark-dashboard-modal-backdrop" role="presentation" onMouseDown={close}><section className="benchmark-dashboard-modal" role="dialog" aria-modal="true" aria-labelledby="dashboard-customizer-title" onMouseDown={(event) => event.stopPropagation()} onKeyDown={(event) => { if (event.key === 'Escape') close(); }}><h2 id="dashboard-customizer-title">Customize benchmark dashboard</h2><p>{draft.length} of {widgets.length} available widgets selected.</p>{Object.entries(groups).map(([category, entries]) => <fieldset key={category}><legend>{category}</legend><button type="button" onClick={() => setDraft((current) => [...new Set([...current, ...entries.map((item) => item.widget_id)])])}>Select all</button><button type="button" onClick={() => setDraft((current) => current.filter((id) => !entries.some((item) => item.widget_id === id)))}>Clear</button>{entries.map((widget) => <label key={widget.widget_id}><input type="checkbox" checked={draft.includes(widget.widget_id)} onChange={() => setDraft((current) => current.includes(widget.widget_id) ? current.filter((id) => id !== widget.widget_id) : [...current, widget.widget_id])} />{widget.label} — {widget.description} ({widget.unit}, {widget.visualization})</label>)}</fieldset>)}<footer><button type="button" onClick={() => setDraft(widgets.filter((item) => item.default_visible).map((item) => item.widget_id))}>Reset to defaults</button><button type="button" onClick={close}>Cancel</button><button type="button" className="primary-button" disabled={!draft.length} onClick={() => { onApply(draft); close(); }}>Apply</button></footer></section></div> };

const CrossBenchmarkPage = () => {
  const workspace = useBenchmarkWorkspace(); const [wizardOpen, setWizardOpen] = useState(false); const [customizing, setCustomizing] = useState(false); const customizeButton = useRef<HTMLButtonElement>(null); const layout = useBenchmarkDashboardLayout(workspace.activeReport?.dashboard); const failed = workspace.activeReport?.tokenizer_results.filter((item) => item.status === 'failed') ?? [];
  const sensors = useSensors(useSensor(PointerSensor, { activationConstraint: { distance: 8 } }), useSensor(KeyboardSensor, { coordinateGetter: sortableKeyboardCoordinates }));
  const handleDragEnd = ({ active, over }: DragEndEvent) => { const next = moveDashboardWidget(layout.resolved.orderedWidgetIds, String(active.id), over ? String(over.id) : null); if (next !== layout.resolved.orderedWidgetIds) layout.setOrder(next); };
  const payload = workspace.activeReport ? { report: workspace.activeReport, visible_widget_ids: layout.resolved.visibleWidgetIds, ordered_widget_ids: layout.resolved.orderedWidgetIds } : null;
  return <section className="page-content cross-benchmark-page"><header className="page-header"><div><p className="eyebrow">Benchmark workspace</p><h1>Cross-benchmark dashboard</h1></div><div className="dashboard-export-header-actions"><button ref={customizeButton} type="button" className="icon-button subtle" disabled={!workspace.activeReport || workspace.loadingReport} title="Customize benchmark dashboard" aria-label="Customize benchmark dashboard" onClick={() => setCustomizing(true)}>⚙</button><DashboardExportButton dashboardType="benchmark" reportName={workspace.activeReport?.run_name ?? 'benchmark-dashboard'} dashboardPayload={payload} /><button type="button" className="primary-button" onClick={() => setWizardOpen(true)}>Run benchmark</button></div></header>{workspace.error && <DismissibleBanner message={workspace.error} onDismiss={workspace.clearError} />}{workspace.loadingPage ? <p>Loading benchmark workspace…</p> : <><label className="field-label">Saved report<select value={workspace.selectedReportId ?? ''} onChange={(event) => { const id = Number(event.target.value); if (id) void workspace.loadReportById(id); }}><option value="">Select a report</option>{workspace.reports.map((report) => <option key={report.report_id} value={report.report_id}>{report.run_name || report.dataset_name}</option>)}</select></label>{workspace.activeReport && <><div className="cross-benchmark-overview-grid"><article className="cross-benchmark-kpi-card"><span>Dataset</span><strong>{workspace.activeReport.dataset_name}</strong></article><article className="cross-benchmark-kpi-card"><span>Documents</span><strong>{workspace.activeReport.documents_processed}</strong></article><article className="cross-benchmark-kpi-card"><span>Tokenizers</span><strong>{workspace.activeReport.tokenizers_count}</strong></article></div>{layout.visibleWidgets.length ? <DndContext sensors={sensors} collisionDetection={closestCenter} onDragEnd={handleDragEnd}><SortableContext items={layout.visibleWidgets.map((widget) => widget.widget_id)} strategy={rectSortingStrategy}><div className="benchmark-dashboard-grid" aria-label="Benchmark metric widgets. Drag a widget or use Space then arrow keys to reorder it.">{layout.visibleWidgets.map((widget) => <SortableWidget key={widget.widget_id} widget={widget} />)}</div></SortableContext></DndContext> : <p className="empty-state">No metric widgets are selected. Customize the dashboard to show one or more calculated metrics.</p>}<article className="cross-benchmark-drilldown-card"><h2>Run diagnostics</h2>{failed.length ? failed.map((item) => <p key={item.tokenizer}>{item.tokenizer}: {item.error_type ?? 'Failed'} — {item.error_message ?? 'No message'}</p>) : <p>No tokenizer failures recorded for this run.</p>}</article></>}</>}{wizardOpen && <BenchmarkRunWizard isOpen={wizardOpen} categories={workspace.metricCategories} availableTokenizers={workspace.tokenizers} availableDatasets={workspace.datasets} defaultDatasetName={workspace.datasets[0] ?? null} defaultMaxDocuments={1000} running={workspace.runningBenchmark} onClose={() => setWizardOpen(false)} onRun={async (runPayload: BenchmarkRunPayload) => { await workspace.runFromWizard(runPayload); }} />}{customizing && <Customizer widgets={layout.widgets} visibleIds={layout.resolved.visibleWidgetIds} onApply={layout.apply} onClose={() => setCustomizing(false)} trigger={customizeButton} />}</section>;
};
export default CrossBenchmarkPage;
