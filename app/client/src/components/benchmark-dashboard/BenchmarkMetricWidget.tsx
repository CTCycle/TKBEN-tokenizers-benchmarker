import { Bar, BarChart, CartesianGrid, Cell, ErrorBar, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import type { BenchmarkDashboardWidgetData, BenchmarkVisualizationKind } from '../../types/api';
import { formatBenchmarkValue } from '../../features/benchmark-dashboard/benchmarkDashboardFormatters';
import { CHART_AXIS_PROPS, CHART_AXIS_TICK, CHART_COLORS, CHART_GRID_PROPS, CHART_SERIES_COLORS, CHART_TOOLTIP_STYLE } from '../../common/chartStyles';

type Props = { widget: BenchmarkDashboardWidgetData; visualization: BenchmarkVisualizationKind; onVisualizationChange?: (visualization: BenchmarkVisualizationKind) => void };
const shortLabel = (value: string) => value.length > 16 ? `${value.slice(0, 14)}…` : value;
const colorFor = (value: string) => CHART_SERIES_COLORS[[...value].reduce((sum, char) => sum + char.charCodeAt(0), 0) % CHART_SERIES_COLORS.length];
const chartMargin = { top: 10, right: 18, left: 8, bottom: 30 };
const visualizationLabels: Record<BenchmarkVisualizationKind, string> = { bar: 'Vertical bar chart', horizontal_bar: 'Horizontal bar chart', interval_bar: 'Interval bar chart', dot_whisker: 'Forest plot', box_plot: 'Box plot', histogram: 'Histogram', grouped_bar: 'Grouped bar chart', heatmap: 'Heatmap' };

const ChartTooltip = ({ active, payload, label, format }: { active?: boolean; payload?: Array<{ name?: string; value?: number; color?: string; payload?: { fullName?: string; unit?: string } }>; label?: string; format: string }) => {
  if (!active || !payload?.length) return null;
  return <div className="benchmark-chart-tooltip"><strong>{payload[0].payload?.fullName ?? label}</strong>{payload.map((item) => <span key={item.name ?? String(item.value)} style={{ color: item.color }}>{item.name ? `${item.name}: ` : ''}{formatBenchmarkValue(Number(item.value), format)}</span>)}</div>;
};

const RechartsMetric = ({ widget, visualization }: Props) => {
  const data = widget.points.map((item) => ({ name: shortLabel(item.tokenizer), fullName: item.tokenizer, value: item.value, error: item.interval_low === null || item.interval_high === null ? [0, 0] : [item.value - item.interval_low, item.interval_high - item.value], color: colorFor(item.tokenizer) }));
  const horizontal = visualization === 'horizontal_bar';
  const interval = visualization === 'interval_bar';
  return <ResponsiveContainer width="100%" height="100%"><BarChart data={data} layout={horizontal ? 'vertical' : 'horizontal'} margin={chartMargin}><CartesianGrid {...CHART_GRID_PROPS} /><XAxis dataKey={horizontal ? 'value' : 'name'} type={horizontal ? 'number' : 'category'} tick={CHART_AXIS_TICK} axisLine={CHART_AXIS_PROPS} label={{ value: horizontal ? widget.unit : undefined, position: 'insideBottom', offset: -18, fill: CHART_COLORS.axisTick }} /><YAxis dataKey={horizontal ? 'name' : 'value'} type={horizontal ? 'category' : 'number'} tick={CHART_AXIS_TICK} axisLine={CHART_AXIS_PROPS} label={{ value: horizontal ? undefined : widget.unit, angle: -90, position: 'insideLeft', fill: CHART_COLORS.axisTick }} /><Tooltip content={<ChartTooltip format={widget.display_format} />} contentStyle={CHART_TOOLTIP_STYLE} /><Bar dataKey="value" name={widget.unit} radius={horizontal ? [0, 4, 4, 0] : [4, 4, 0, 0]}>{data.map((item) => <Cell key={item.fullName} fill={item.color} />)}{interval && <ErrorBar dataKey="error" width={5} stroke={CHART_COLORS.yellow} />}</Bar></BarChart></ResponsiveContainer>;
};

const ForestPlot = ({ widget }: { widget: BenchmarkDashboardWidgetData }) => {
  const values = widget.points.flatMap((item) => [item.interval_low ?? item.value, item.interval_high ?? item.value, item.value]);
  const min = Math.min(...values, 0); const max = Math.max(...values, 1); const scale = (value: number) => 176 + ((value - min) / Math.max(max - min, 1)) * 448;
  const height = Math.max(240, widget.points.length * 48 + 52);
  return <svg className="benchmark-custom-chart" viewBox={`0 0 680 ${height}`} role="img" aria-label={`${widget.label} forest plot in ${widget.unit}`}><text x="176" y="16" fill={CHART_COLORS.axisTick} fontSize="11">{widget.unit}</text>{widget.points.map((item, index) => { const y = 40 + index * 44; const low = item.interval_low ?? item.value; const high = item.interval_high ?? item.value; return <g key={item.tokenizer}><title>{`${item.tokenizer}: ${formatBenchmarkValue(item.value, widget.display_format)}; interval ${formatBenchmarkValue(low, widget.display_format)}–${formatBenchmarkValue(high, widget.display_format)}`}</title><text x="164" y={y + 4} fill={CHART_COLORS.axisTick} textAnchor="end" fontSize="12">{shortLabel(item.tokenizer)}</text><line x1={scale(low)} x2={scale(high)} y1={y} y2={y} stroke={CHART_COLORS.yellow} strokeWidth="3" /><line x1={scale(low)} x2={scale(low)} y1={y - 7} y2={y + 7} stroke={CHART_COLORS.yellow} /><line x1={scale(high)} x2={scale(high)} y1={y - 7} y2={y + 7} stroke={CHART_COLORS.yellow} /><circle cx={scale(item.value)} cy={y} r="7" fill={colorFor(item.tokenizer)} /></g>; })}</svg>;
};

const BoxPlot = ({ widget }: { widget: BenchmarkDashboardWidgetData }) => {
  const values = widget.distributions.flatMap((item) => [item.min, item.max]).filter(Number.isFinite); const min = Math.min(...values, 0); const max = Math.max(...values, 1); const logarithmic = min > 0 && max / min >= 50; const scaleValue = (value: number) => logarithmic ? Math.log(value / min) / Math.log(max / min) : (value - min) / Math.max(max - min, 1); const scale = (value: number) => 176 + scaleValue(value) * 448; const height = Math.max(240, widget.distributions.length * 54 + 42);
  return <svg className="benchmark-custom-chart" viewBox={`0 0 680 ${height}`} role="img" aria-label={`${widget.label} box plot in ${widget.unit}${logarithmic ? ', log scale' : ''}`}>{logarithmic && <text x="176" y="16" fill={CHART_COLORS.axisTick} fontSize="11">Log scale · {widget.unit}</text>}{widget.distributions.map((item, index) => { const y = 40 + index * 54; return <g key={item.tokenizer}><title>{`${item.tokenizer}: min ${formatBenchmarkValue(item.min, widget.display_format)}, Q1 ${formatBenchmarkValue(item.q1, widget.display_format)}, median ${formatBenchmarkValue(item.median, widget.display_format)}, Q3 ${formatBenchmarkValue(item.q3, widget.display_format)}, max ${formatBenchmarkValue(item.max, widget.display_format)}`}</title><text x="165" y={y + 4} fill={CHART_COLORS.axisTick} textAnchor="end" fontSize="12">{shortLabel(item.tokenizer)}</text><line x1={scale(item.min)} x2={scale(item.max)} y1={y} y2={y} stroke={CHART_COLORS.axis} strokeWidth="2" /><line x1={scale(item.min)} x2={scale(item.min)} y1={y - 9} y2={y + 9} stroke={CHART_COLORS.axis} /><line x1={scale(item.max)} x2={scale(item.max)} y1={y - 9} y2={y + 9} stroke={CHART_COLORS.axis} /><rect x={scale(item.q1)} y={y - 11} width={Math.max(scale(item.q3) - scale(item.q1), 2)} height="22" rx="4" fill={colorFor(item.tokenizer)} fillOpacity="0.78" /><line x1={scale(item.median)} x2={scale(item.median)} y1={y - 12} y2={y + 12} stroke={CHART_COLORS.yellow} strokeWidth="3" /></g>; })}</svg>;
};

const HistogramSmallMultiples = ({ widget }: { widget: BenchmarkDashboardWidgetData }) => {
  const tokenizers = [...new Set(widget.histogram_bins.map((item) => item.tokenizer))]; const edges = [...new Set(widget.histogram_bins.map((item) => item.bin_low))].sort((a, b) => a - b); const lastHigh = widget.histogram_bins.reduce((high, item) => Math.max(high, item.bin_high), 0); const bins = [...edges, lastHigh]; const maxCount = Math.max(...widget.histogram_bins.map((item) => item.count), 1);
  return <div className="benchmark-histogram-grid" aria-label={`${widget.label} histogram small multiples`}>{tokenizers.map((tokenizer) => <div className="benchmark-histogram-small-multiple" key={tokenizer}><strong>{tokenizer}</strong><svg className="benchmark-custom-chart" viewBox="0 0 320 190" role="img" aria-label={`${widget.label} for ${tokenizer} in ${widget.unit}`}><title>{`${tokenizer} histogram · ${widget.unit}`}</title>{bins.slice(0, -1).map((low, index) => { const high = bins[index + 1]; const bin = widget.histogram_bins.find((item) => item.tokenizer === tokenizer && item.bin_low === low); const count = bin?.count ?? 0; const x = 24 + index * (272 / Math.max(bins.length - 1, 1)); const width = Math.max(2, 272 / Math.max(bins.length - 1, 1) - 1); const height = (count / maxCount) * 134; return <rect key={`${tokenizer}-${low}`} x={x} y={160 - height} width={width} height={height} fill={colorFor(tokenizer)}><title>{`${tokenizer}: ${low}–${high}, ${count} observations (${((bin?.proportion ?? 0) * 100).toFixed(1)}%)`}</title></rect>; })}<line x1="24" x2="296" y1="160" y2="160" stroke={CHART_COLORS.axis} /><text x="24" y="178" fill={CHART_COLORS.axisTick} fontSize="10">{formatBenchmarkValue(bins[0] ?? 0, widget.display_format)}</text><text x="296" y="178" textAnchor="end" fill={CHART_COLORS.axisTick} fontSize="10">{formatBenchmarkValue(bins.at(-1) ?? 0, widget.display_format)}</text></svg></div>)}</div>;
};

const GroupedBar = ({ widget }: { widget: BenchmarkDashboardWidgetData }) => { const buckets = [...new Set(widget.buckets.map((item) => item.bucket))]; const tokenizers = [...new Set(widget.buckets.map((item) => item.tokenizer))]; const data = buckets.map((bucket) => Object.fromEntries([['name', shortLabel(bucket)], ['fullName', bucket], ...tokenizers.map((tokenizer) => [tokenizer, widget.buckets.find((item) => item.bucket === bucket && item.tokenizer === tokenizer)?.value ?? null])])) as Array<Record<string, string | number | null>>; return <ResponsiveContainer width="100%" height="100%"><BarChart data={data} margin={chartMargin}><CartesianGrid {...CHART_GRID_PROPS} /><XAxis dataKey="name" tick={CHART_AXIS_TICK} axisLine={CHART_AXIS_PROPS} /><YAxis tick={CHART_AXIS_TICK} axisLine={CHART_AXIS_PROPS} label={{ value: widget.unit, angle: -90, position: 'insideLeft', fill: CHART_COLORS.axisTick }} /><Tooltip content={<ChartTooltip format={widget.display_format} />} contentStyle={CHART_TOOLTIP_STYLE} />{tokenizers.map((tokenizer) => <Bar key={tokenizer} dataKey={tokenizer} name={tokenizer} fill={colorFor(tokenizer)} radius={[3, 3, 0, 0]} />)}</BarChart></ResponsiveContainer>; };

const Heatmap = ({ widget }: { widget: BenchmarkDashboardWidgetData }) => { const tokenizers = [...new Set(widget.buckets.map((item) => item.tokenizer))]; const buckets = [...new Set(widget.buckets.map((item) => item.bucket))]; const byKey = new Map(widget.buckets.map((item) => [`${item.tokenizer}-${item.bucket}`, item.value])); const values = widget.buckets.map((item) => item.value); const min = Math.min(...values); const max = Math.max(...values); const intensity = (value: number) => (value - min) / Math.max(max - min, 1); return <div className="benchmark-heatmap-wrap"><div className="benchmark-heatmap" role="grid" aria-label={`${widget.label} heatmap`} style={{ gridTemplateColumns: `minmax(110px, 1.2fr) repeat(${buckets.length}, minmax(64px, 1fr))` }}><span className="benchmark-heatmap-corner">Tokenizer / bucket</span>{buckets.map((bucket) => <strong key={bucket}>{shortLabel(bucket)}</strong>)}{tokenizers.map((tokenizer) => <span className="benchmark-heatmap-row" role="row" key={`${tokenizer}-row`}><strong>{shortLabel(tokenizer)}</strong>{buckets.map((bucket) => { const value = byKey.get(`${tokenizer}-${bucket}`); return <span key={`${tokenizer}-${bucket}`} className="benchmark-heatmap-cell" role="gridcell" tabIndex={0} style={{ backgroundColor: value === undefined ? CHART_COLORS.tooltipBg : `color-mix(in srgb, ${CHART_COLORS.blue} ${Math.round(20 + intensity(value) * 70)}%, ${CHART_COLORS.tooltipBg})` }} aria-label={`${tokenizer}, ${bucket}: ${value === undefined ? 'not available' : `${formatBenchmarkValue(value, widget.display_format)} ${widget.unit}`}`}>{value === undefined ? '—' : formatBenchmarkValue(value, widget.display_format)}</span>; })}</span>)}</div><div className="benchmark-heatmap-legend" aria-label={`Heatmap scale from ${formatBenchmarkValue(min, widget.display_format)} to ${formatBenchmarkValue(max, widget.display_format)}`}><span>Low</span><i /><span>High</span><small>{formatBenchmarkValue(min, widget.display_format)}–{formatBenchmarkValue(max, widget.display_format)} {widget.unit}</small></div></div>; };

const WidgetDataTable = ({ widget }: { widget: BenchmarkDashboardWidgetData }) => { if (widget.histogram_bins.length) return <details className="benchmark-dashboard-data-table"><summary>View data table</summary><div><table><caption>{widget.label} histogram bins</caption><thead><tr><th scope="col">Tokenizer</th><th scope="col">Bin low</th><th scope="col">Bin high</th><th scope="col">Count</th><th scope="col">Proportion</th></tr></thead><tbody>{widget.histogram_bins.map((item) => <tr key={`${item.tokenizer}-${item.bin_low}-${item.bin_high}`}><th scope="row">{item.tokenizer}</th><td>{formatBenchmarkValue(item.bin_low, widget.display_format)}</td><td>{formatBenchmarkValue(item.bin_high, widget.display_format)}</td><td>{item.count.toLocaleString()}</td><td>{(item.proportion * 100).toFixed(1)}%</td></tr>)}</tbody></table></div></details>; if (widget.distributions.length) return <details className="benchmark-dashboard-data-table"><summary>View data table</summary><div><table><caption>{widget.label} distribution values</caption><thead><tr><th scope="col">Tokenizer</th><th scope="col">Min</th><th scope="col">Q1</th><th scope="col">Median</th><th scope="col">Q3</th><th scope="col">Max</th><th scope="col">Samples</th></tr></thead><tbody>{widget.distributions.map((item) => <tr key={item.tokenizer}><th scope="row">{item.tokenizer}</th><td>{formatBenchmarkValue(item.min, widget.display_format)}</td><td>{formatBenchmarkValue(item.q1, widget.display_format)}</td><td>{formatBenchmarkValue(item.median, widget.display_format)}</td><td>{formatBenchmarkValue(item.q3, widget.display_format)}</td><td>{formatBenchmarkValue(item.max, widget.display_format)}</td><td>{item.sample_count.toLocaleString()}</td></tr>)}</tbody></table></div></details>; if (widget.buckets.length) return <details className="benchmark-dashboard-data-table"><summary>View data table</summary><div><table><caption>{widget.label} values by bucket</caption><thead><tr><th scope="col">Tokenizer</th><th scope="col">Bucket</th><th scope="col">Value ({widget.unit})</th></tr></thead><tbody>{widget.buckets.map((item) => <tr key={`${item.tokenizer}-${item.bucket}`}><th scope="row">{item.tokenizer}</th><td>{item.bucket}</td><td>{formatBenchmarkValue(item.value, widget.display_format)}</td></tr>)}</tbody></table></div></details>; const hasIntervals = widget.points.some((item) => item.interval_low !== null && item.interval_high !== null); return <details className="benchmark-dashboard-data-table"><summary>View data table</summary><div><table><caption>{widget.label} values by tokenizer</caption><thead><tr><th scope="col">Tokenizer</th><th scope="col">Value ({widget.unit})</th>{hasIntervals && <><th scope="col">Interval low</th><th scope="col">Interval high</th></>}</tr></thead><tbody>{widget.points.map((item) => <tr key={item.tokenizer}><th scope="row">{item.tokenizer}</th><td>{formatBenchmarkValue(item.value, widget.display_format)}</td>{hasIntervals && <><td>{item.interval_low === null ? 'N/A' : formatBenchmarkValue(item.interval_low, widget.display_format)}</td><td>{item.interval_high === null ? 'N/A' : formatBenchmarkValue(item.interval_high, widget.display_format)}</td></>}</tr>)}</tbody></table></div></details>; };

const Chart = ({ widget, visualization }: Props) => { if (!widget.points.length && !widget.distributions.length && !widget.buckets.length) return <p className="benchmark-dashboard-empty">No metric data available.</p>; if (visualization === 'box_plot') return <BoxPlot widget={widget} />; if (visualization === 'histogram') return <HistogramSmallMultiples widget={widget} />; if (visualization === 'dot_whisker') return <ForestPlot widget={widget} />; if (visualization === 'grouped_bar') return <GroupedBar widget={widget} />; if (visualization === 'heatmap') return <Heatmap widget={widget} />; return <RechartsMetric widget={widget} visualization={visualization} />; };

export const BenchmarkMetricWidget = ({ widget, visualization, onVisualizationChange }: Props) => <article className={`benchmark-dashboard-widget benchmark-dashboard-widget--${widget.width}`} aria-label={`${widget.label} widget`}><header><div><h3>{widget.label}</h3><p className="cross-benchmark-chart-note">{widget.description} ({widget.unit})</p></div><div className="benchmark-visualization-switcher" role="group" aria-label={`${widget.label} visualization`} onPointerDown={(event) => event.stopPropagation()} onKeyDown={(event) => event.stopPropagation()}>{widget.compatible_visualizations.map((choice) => <button key={choice} type="button" className="benchmark-visualization-button" aria-label={`Use ${visualizationLabels[choice]} for ${widget.label}`} title={visualizationLabels[choice]} aria-pressed={choice === visualization} onClick={() => onVisualizationChange?.(choice)}>{choice === 'bar' && '▮'}{choice === 'horizontal_bar' && '▰'}{choice === 'interval_bar' && '▥'}{choice === 'dot_whisker' && '⊙'}{choice === 'box_plot' && '▣'}{choice === 'histogram' && '▥'}{choice === 'grouped_bar' && '▤'}{choice === 'heatmap' && '▦'}</button>)}</div></header><div className="benchmark-chart-stage"><Chart widget={widget} visualization={visualization} /></div><WidgetDataTable widget={widget} /></article>;
