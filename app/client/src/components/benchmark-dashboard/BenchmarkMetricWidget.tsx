import { Bar, BarChart, CartesianGrid, ErrorBar, ResponsiveContainer, Tooltip, XAxis, YAxis } from 'recharts';
import type { BenchmarkDashboardWidgetData } from '../../types/api';
import { formatBenchmarkValue } from '../../features/benchmark-dashboard/benchmarkDashboardFormatters';

type Props = { widget: BenchmarkDashboardWidgetData };
const BoxPlot = ({ widget }: Props) => <div className="benchmark-dashboard-boxes">{widget.distributions.map((item) => <p key={item.tokenizer}><strong>{item.tokenizer}</strong><span>{formatBenchmarkValue(item.min, widget.display_format)} / {formatBenchmarkValue(item.q1, widget.display_format)} / {formatBenchmarkValue(item.median, widget.display_format)} / {formatBenchmarkValue(item.q3, widget.display_format)} / {formatBenchmarkValue(item.max, widget.display_format)}</span></p>)}</div>;
const WidgetDataTable = ({ widget }: Props) => {
  if (widget.visualization === 'box_plot') return <details className="benchmark-dashboard-data-table"><summary>View data table</summary><div><table><caption>{widget.label} distribution values</caption><thead><tr><th scope="col">Tokenizer</th><th scope="col">Min</th><th scope="col">Q1</th><th scope="col">Median</th><th scope="col">Q3</th><th scope="col">Max</th><th scope="col">Samples</th></tr></thead><tbody>{widget.distributions.map((item) => <tr key={item.tokenizer}><th scope="row">{item.tokenizer}</th><td>{formatBenchmarkValue(item.min, widget.display_format)}</td><td>{formatBenchmarkValue(item.q1, widget.display_format)}</td><td>{formatBenchmarkValue(item.median, widget.display_format)}</td><td>{formatBenchmarkValue(item.q3, widget.display_format)}</td><td>{formatBenchmarkValue(item.max, widget.display_format)}</td><td>{item.sample_count.toLocaleString()}</td></tr>)}</tbody></table></div></details>;
  if (widget.visualization === 'bucket_bar') return <details className="benchmark-dashboard-data-table"><summary>View data table</summary><div><table><caption>{widget.label} values by bucket</caption><thead><tr><th scope="col">Tokenizer</th><th scope="col">Bucket</th><th scope="col">Value ({widget.unit})</th></tr></thead><tbody>{widget.buckets.map((item) => <tr key={`${item.tokenizer}-${item.bucket}`}><th scope="row">{item.tokenizer}</th><td>{item.bucket}</td><td>{formatBenchmarkValue(item.value, widget.display_format)}</td></tr>)}</tbody></table></div></details>;
  return <details className="benchmark-dashboard-data-table"><summary>View data table</summary><div><table><caption>{widget.label} values by tokenizer</caption><thead><tr><th scope="col">Tokenizer</th><th scope="col">Value ({widget.unit})</th>{widget.visualization === 'interval_bar' && <><th scope="col">Interval low</th><th scope="col">Interval high</th></>}</tr></thead><tbody>{widget.points.map((item) => <tr key={item.tokenizer}><th scope="row">{item.tokenizer}</th><td>{formatBenchmarkValue(item.value, widget.display_format)}</td>{widget.visualization === 'interval_bar' && <><td>{item.interval_low === null ? 'N/A' : formatBenchmarkValue(item.interval_low, widget.display_format)}</td><td>{item.interval_high === null ? 'N/A' : formatBenchmarkValue(item.interval_high, widget.display_format)}</td></>}</tr>)}</tbody></table></div></details>;
};
export const BenchmarkMetricWidget = ({ widget }: Props) => {
  const isBucket = widget.visualization === 'bucket_bar';
  const data = isBucket ? widget.buckets.map((item) => ({ name: `${item.tokenizer} · ${item.bucket}`, value: item.value })) : widget.points.map((item) => ({ name: item.tokenizer, value: item.value, interval: item.interval_low === null || item.interval_high === null ? [0, 0] : [Math.max(0, item.value - item.interval_low), Math.max(0, item.interval_high - item.value)] }));
  return <article className={`benchmark-dashboard-widget benchmark-dashboard-widget--${widget.width}`} aria-label={`${widget.label} widget`}><header><h3>{widget.label}</h3><p className="cross-benchmark-chart-note">{widget.description} ({widget.unit})</p></header>{widget.visualization === 'box_plot' ? <BoxPlot widget={widget} /> : <ResponsiveContainer width="100%" height={260}><BarChart data={data}><CartesianGrid strokeDasharray="3 3" /><XAxis dataKey="name" interval={0} angle={-18} textAnchor="end" height={54} /><YAxis /><Tooltip formatter={(value) => formatBenchmarkValue(Number(value), widget.display_format)} />{widget.visualization === 'interval_bar' ? <Bar dataKey="value" fill="#38bdf8"><ErrorBar dataKey="interval" width={4} strokeWidth={1.5} /></Bar> : <Bar dataKey="value" fill={isBucket ? '#a78bfa' : '#38bdf8'} />}</BarChart></ResponsiveContainer>}<WidgetDataTable widget={widget} /></article>;
};
